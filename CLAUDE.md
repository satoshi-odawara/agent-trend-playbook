# agent-trend-playbook

agent-trend-data に蓄積されたサーベイデータから「試す価値がありそうなもの」を
選び、実際に自分で使って有効性を検証し、その結果を記事として発信するリポジトリ。

「世の中でどう使われているか」(データ)と「自分で確かめたらどうだったか」(検証)
の両方を揃えることで、感想ではなくエビデンスに基づいた実践ガイドとして発信する
ことを狙う。

## 目的・差別化
- データが示す傾向を、自分で使って検証したうえで「実際にこう使うと良い」という
  実践知の形に落として発信する
- 検証ログ(verifications/)の蓄積そのものがこのリポジトリ独自の資産。
  hubの時系列データが「世の中の動き」の証拠、こちらは「自分で確かめた」証拠

## 3リポジトリ構成における位置づけ
役割ごとにAIエージェントのコンテキストを分離するため、意図的に3つに分けている。

- **agent-trend-radar**: データ収集システム。GitHub APIで公開リポジトリを調査
- **agent-trend-data**: 公開データリポジトリ(ハブ)。データと記事原稿の受け渡し場所
- **agent-trend-playbook**: 本リポジトリ。検証と記事作成

このリポジトリは agent-trend-radar には一切関与しない。データが欲しい場合は
後述の「データへの要望」の手順に従う。

## パイプライン
1. **候補抽出**: `agent-trend-data/latest/metrics.json` から、試す価値がありそうな
   パターン・ツールを拾い `candidates/` に置く
2. **検証**: 実際に手を動かして試す。**ここは人間主導**。結果を `verifications/` に
   軽量ログとして残す
3. **記事ドラフト生成**: 検証ログ + hubのデータを入力に、記事の初稿を `drafts/` に書く
4. **公開**: 完成原稿を `agent-trend-data/articles/` に反映する

## 現在のスコープ(MVP)
- **まず1本、完全手動で最後まで通す**ことを優先する。自動化はその後
- 1本通してみて、どの工程に時間がかかったかを見てから機能を足す
- この段階で作り込まないもの: 候補抽出の自動化、記事生成の自動化、
  hubへの自動push、GitHub Actions

## ディレクトリ構成
```
candidates/       # 検証してみたい候補のストック(YYYY-MM-DD-topic.md)
verifications/    # 実際にやってみた記録(YYYY-MM-DD-topic/log.md)
drafts/           # 記事の下書き。完成後にhubのarticles/へ
```

## 検証ログの方針
- **重厚なレポートにしない**。平日夜1〜2時間の制約下で続けられる粒度を守る
- 最低限、以下が残っていればよい
  - 何を試したか
  - どういう結果だったか(できれば再現できる形で)
  - 所感(期待と違った点、実際に使えそうかどうか)
- 検証ログには、根拠にした `agent-trend-data` のスナップショット日付を記載し、
  トレーサビリティを確保する

## データへの要望
検証や記事作成の過程で「このデータがあれば、もっと強い主張ができた」と気づいた
場合、`agent-trend-data/data-requests/pending/` にテンプレートに沿ったMarkdownを
追加する。

- このリポジトリから agent-trend-radar に直接Issueを立てたり書き込んだりしない
- 要望を書くだけ。採否とIssue化は人間が判断する

## 制約
- 低コスト運用必須。有料SaaS・高額API課金は避ける
- SCSK九州の業務知見・機密情報を一切含めない。個人の技術検証・OSSとして
  クリーンに進める

## 進め方
- Issueで作業単位を管理する
- 実装はClaude Codeに委譲する。方針検討・データ解釈はClaudeチャット
  (Projects)側で行う

---

  LLMによる一般的なコーディングミスを減らすための行動指針です。

**トレードオフ:** これらの指針は、スピードよりも「慎重さ」を優先しています。些細なタスクについては、状況に応じて判断してください。

## 1. コードを書く前に考える

**仮定で進めない。疑問をうやむやにしない。トレードオフを明確にする。**

実装する前に：

* 自分の仮定を明示してください。不確実な点があれば質問してください。
* 複数の解釈ができる場合は、勝手に判断せず、それらを提示してください。
* よりシンプルなアプローチがある場合は、それを伝えてください。正当な理由があれば、提案に異議を唱えて（プッシュバックして）ください。
* 不明瞭な点がある場合は、手を止めてください。何が混乱の原因なのかを特定し、質問してください。

## 2. シンプルさ最優先

**問題を解決する最小限のコード。推測による実装はしない。**

* 要求されていない機能は追加しない。
* 1回しか使わないコードに対して、抽象化を行わない。
* 要求されていない「柔軟性」や「設定変更のしやすさ（設定可能性）」を持たせない。
* 起こり得ないシナリオに対するエラーハンドリングは書かない。
* 50行で済むところを200行書いてしまった場合は、書き直してください。

「シニアエンジニアが見たら『複雑すぎる』と言うだろうか？」と自問してください。もしそうなら、シンプルにしてください。

## 3. 外科手術のようなピンポイントの変更

**必要な部分だけを触る。自分が触った部分の始末だけをつける。**

既存のコードを編集する時：

* 隣接するコード、コメント、フォーマットなどを「改善」しようとしないでください。
* 壊れていないものをリファクタリングしないでください。
* 自分の好みとは違っていても、既存のスタイルに合わせてください。
* 関係のないデッドコード（使われていないコード）に気づいた場合は、指摘するにとどめ、削除はしないでください。

自分の変更によって使われなくなったコード（オーファン）が生じた時：

* **自分の**変更によって不必要になったインポート、変数、関数は削除してください。
* 指示がない限り、以前から存在していたデッドコードは削除しないでください。

評価基準：変更されたすべての行が、ユーザーの要求に直接結びついていること。

## 4. ゴール駆動の実行

**成功基準を定義する。検証されるまでループを回す。**

タスクを検証可能なゴールに変換してください：

* 「バリデーションを追加する」 → 「無効な入力に対するテストを書き、それをパスさせる」
* 「バグを修正する」 → 「バグを再現するテストを書き、それをパスさせる」
* 「Xをリファクタリングする」 → 「前後のテストがパスすることを確認する」
